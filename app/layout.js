import './globals.css'
export const metadata = { title: 'Invoice SaaS Pro', description: 'Aplikasi Invoice Profesional' }
export default function RootLayout({ children }) {
  return (<html lang='id'><body>{children}</body></html>)
}
